apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "aerowork-dashboard.fullname" . }}-test-connection"
  labels:
    {{- include "aerowork-dashboard.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "aerowork-dashboard.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
